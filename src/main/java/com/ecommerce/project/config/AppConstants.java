package com.ecommerce.project.config;

/**
 * AppConstants class provides application-wide constant values
 * used for default configurations such as pagination, sorting, and ordering.
 *
 * These constants serve as default values for handling API requests,
 * ensuring consistent behavior across endpoints.
 *
 * Examples include default page size, sorting attributes, and sort order.
 *
 * @author Adithya R
 */
public class AppConstants {
    public static final String PAGE_NUMBER = "0";
    public static final String PAGE_SIZE = "50";
    public static final String SORT_CATEGORIES_BY = "categoryId";
    public static final String SORT_DIR = "asc";
    public static final String SORT_PRODUCTS_BY = "productId";
}
